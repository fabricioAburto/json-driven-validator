/**
 * ### Validation Rules Builder - Helper
 *
 * Just help clients to get intellisense help for create the
 * rules object config.
 *
 * @author Jose Aburto <pino0071@gmail.com>
 *
 * @param {import("..").ValidationRulesTypes} definition
 * @returns {import("..").ValidationRules}
 */
export function validationRulesBuilder(definition) {
  return definition;
}
